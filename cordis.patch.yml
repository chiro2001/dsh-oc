# 叠加在 dsh-base 之上；插入顺序决定依赖激活顺序。
- id: session-title-llm
  disabled: true

- insert:
    - id: webserver
      name: '@deepseek-ai/dsh-host-webserver'
      config:
        host: 127.0.0.1
        port: 0

    - id: agent-presets
      name: '@deepseek-ai/dsh-agent-presets'
      config:
        default: minimal

    # The standard preset's delegation tools need the host model-selection
    # settings namespace, which dsh-base does not mount by itself.
    - id: subagent-model-selection-settings
      name: '@deepseek-ai/dsh-tool-subagent/model-selection-settings'

    - id: workspace
      name: '@deepseek-ai/dsh-workspace'

    - id: directory-picker
      name: '@deepseek-ai/dsh-host-directory-picker-auto'

    - id: session-controller
      name: '@deepseek-ai/dsh-api-session-controller'

    - id: oc-bridge
      name: '@chiro2001/dsh-oc/bridge'
      inject: [sessionController, agentPresets, goals, sessionSkillCatalog, agents, sessions, sessionProjections]

    - id: oc-tui
      name: '@chiro2001/dsh-oc/tui'
      inject: [ocBridge]
