# 性能基准 — 5000 sessions（2026-08-17）

commit `609ccd6`，本机 Linux x64，opencode 1.18.18，`node scripts/perf.mjs
--sessions 5000`。数据规模 24.68 MiB（5000 会话，每会话 3 条消息）。

| 指标 | p50 | p95 |
|---|---:|---:|
| GET /session (v1) | 22.4ms | 1383.9ms |
| GET /api/session (v2) | 4.0ms | 39.1ms |
| GET /session/:id/message (v1) | 1907.5ms | 4751.6ms |
| GET /api/session/:id/message (v2) | 1.1ms | 3.4ms |
| GET /api/session/:id/history (v2) | 1.1ms | 1845.0ms |
| history 分页（6 条） | 1 页 / 6 条 / 0.99ms | — |

其他：

- 标题补温：initial 19/5000 → afterWarm 25/5000（3s 窗口，只补最近 24 个，
  与文档一致；v1 冷扫首读 ~1.4s 出现在 p95）。
- SSE 首个事件延迟：0.22ms。
- dsh RSS：373,812 kB（约 365 MiB）；bridge 启动：2.81s。

对比 `docs/perf/results-2026-08-15.md`：数值量级一致，无回归。v1 message
冷折叠成本（跨 5000 会话存储扫描）仍集中在 p95，v2/history 走缓存后
毫秒级。
