---
name: Bug 报告
about: 反馈 dsh-oc 的异常行为
title: 'bug: '
labels: bug
---

## 现象

<!-- 发生了什么，期望是什么。 -->

## 复现步骤

1. 安装方式（本地 link / GitHub `#main` / `#develop`）
2. 启动命令（`dsh --profile oc [--mini|--session ...]`）
3. 操作步骤

## 环境

- dsh 版本：`dsh --version`
- dsh-oc 版本：`dsh-oc --version`（或 `dsh --profile oc --help` 首行）
- opencode 二进制：`opencode --version`
- OS / 终端 / tmux 是否使用

## 日志

<!-- 贴 `--print-logs` 输出、`~/.dsh/profiles/oc` 相关报错、会话文件等。 -->
