# Round 0002 prompt — 恢复一致性收尾与 rc.2 发布门槛

你是 dsh-oc 项目的顶级审阅/顾问。dsh-oc 是 dsh（DeepSeek Harness）的
opencode TUI 前端，直接包装官方 opencode 1.18.18 二进制，通过桥接层把 dsh
的会话/工具/权限/goal 协议翻译成 opencode SDK v2 面。round-0001 后已完成
一批恢复一致性修复：历史 part id 与实时 SSE 对齐、工具回合+后续文本历史
合并、后续文本独立 id、history 分页游标；并新增 live vs `--session` 的
规范化消息图一致性 e2e。当前是“恢复一致性收尾 + 决定 rc.2 门槛”的节点。

请阅读 `context.md` 及其中列出的输入，完成以下任务。

## 1. 审阅职责

1. **反驳与批评**：对以下判断给出独立意见——
   - 用“live vs `--session` 规范化消息图一致”作为当前恢复一致性的主证据，
     是否足以收尾；`--continue` 变体是否值得现在加入，还是应该先做断线/
     崩溃重启；
   - 已知错序裁决（工具+忙碌中排队即时视图错序归上游 TUI）是否已足够
     闭合，还是仍需要官方最小复现证据；
   - “mock 全绿 + 一次真实模型 smoke”对 rc.2 的剩余缺口在哪；
   - 直接包装官方二进制 + 桥接翻译的架构，在升级 1.18.18 之后的版本时
     应该怎么守住 ABI 语义（黄金轨迹/语义差分）而不退化成“逐个试”。
2. **最可能遗漏的风险**：`-c`/`--session`/SSE 重连/桥崩溃重启在 dsh 事件
   持久化与 opencode SDK v2 语义上的差异；真实模型会话回放的脱敏与确定性；
   GitHub 源不可变安装/升级/回滚与 `lib/` 一致性；flake 统计的成本控制。
3. **按信息增益排序的 1–3 个“下一步动作/实验”**：输入、动作、可证伪断言、
   停止条件，并给出预期耗时量级（测试时长需可控）。
4. **方向性判断**：rc.2 发布前必须做的最小门槛清单是什么；哪些可延后；
   当前阶段功能面/稳定性面/流程面的投入比例建议。
5. **明确区分**：事实 / 推断 / 需实验。

## 2. 交付物

1. **`recommendation.md`**：审阅结论 + rc.2 门槛清单 + 下一步建议；
2. 最终回复由 CLI `-o` 落盘 `response.md`。

## 3. 约束

- 只写 `expert-advice/round-0002/` 目录。
- 不执行安装/构建/测试/git 写操作；不查看无关仓库。
