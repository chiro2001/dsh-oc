# Round 0001 prompt — dsh-oc 当前状态、近期修复与下一步方向

你是 dsh-oc 项目的顶级审阅/顾问。dsh-oc 是 dsh（DeepSeek Harness）的
opencode TUI 前端，直接包装官方 opencode 1.18.18 二进制，通过桥接层把 dsh
的会话/工具/权限/goal 协议翻译成 opencode SDK v2 面。近期完成了一批显示
与协议修复（首条回复重复、agent preset 标签回退、错误显示、QUEUED 排队、
`/goal` 输入、历史会话标题、Thought 时长、新增 v2 history 端点），并做了
CI 加固（分片、超时、重试、失败工件限时上传）。当前是“稳定后决定下一步
方向”的节点。

请阅读 `context.md` 及其中列出的输入，完成以下任务。

## 1. 审阅职责

1. **反驳与批评**：对以下判断给出独立意见——
   - 当前“mock 全绿 + 真实模型回归通过”是否足以支撑向用户交付/发布；
   - 已知残余（工具+排队转录顺序错位）应继续在桥接层尝试，还是接受为上游
     TUI 行为并文档化；
   - CI 偶发 flake 的处置（重试/稳定帧轮询）是否会掩盖真实回归；
   - 直接包装官方二进制 + 桥接翻译的架构，长期最大风险是什么（升级、
     协议漂移、维护面）。
2. **最可能遗漏的风险**：真实模型行为差异、会话恢复/历史一致性、
   安装/更新流程（GitHub 源直装）、多分支/发布流程、性能（标题补温、
   大会话历史）。
3. **按信息增益排序的 1–3 个“下一步动作/实验”**：输入、动作、可证伪断言、
   停止条件。
4. **方向性判断**：接下来最应该投入的是功能面、稳定性面、还是流程/文档面；
   给出明确建议。
5. **明确区分**：事实 / 推断 / 需实验。

## 2. 交付物

1. **`recommendation.md`**：审阅结论 + 下一步建议清单；
2. 最终回复由 CLI `-o` 落盘 `response.md`。

## 3. 约束

- 只写 `expert-advice/round-0001/` 目录。
- 不执行安装/构建/测试/git 写操作；不查看无关仓库。
